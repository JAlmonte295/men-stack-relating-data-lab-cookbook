<%- include('../partials/header.ejs', { title: 'Cookbook Community' }) %>

    <h1>Community</h1>
    <h2>See what other users have in their pantry!</h2>
    <ul>
      <% users.forEach(function(user) { %>
      <li><a href="/users/<%= user._id %>"><%= user.username %></a></li>
      <% }); %>
    </ul>

<%- include('../partials/footer.ejs') %>
